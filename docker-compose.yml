services:
  postgres:
    image: postgres:16
    container_name: yplaza_db
    environment:
      POSTGRES_DB: yplaza
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: